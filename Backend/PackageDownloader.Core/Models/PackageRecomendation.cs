namespace PackageDownloader.Core.Models;

public record PackageRecommendation(string Name,
                                    string Id,
                                    string ChoiceDescription,
                                    string CodeExample);